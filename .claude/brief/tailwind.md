# Tailwind

- Use Tailwind utility classes directly — no inline styles
- Custom tokens (brand colors, spacing) defined in `tailwind.config.ts`
- Responsive-first: mobile → tablet → desktop (`sm:`, `md:`, `lg:`)
- No arbitrary values unless there is no token equivalent — flag these in a comment
